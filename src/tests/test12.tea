fn mod(a:int, b:int)->int{
    ret a - a/b*b;
}

fn main()->int{
    let a:int = 893;
	let b:int = 716;
	a = 837;
	a = a + 128;
	b = b + a;
	b = 241;
	a = a + b - 412;
	a = 771;
	b = b + a -18;
	a = b + 66;
	b = b + a - 33;
	a = b - 55;
	a = b + a - 97;
	b = mod(b-a, 62);
    ret mod(a+b, 17);
}