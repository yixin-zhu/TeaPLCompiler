let gad[10]:int = {gb+26};

fn fooll();
fn barnull();
fn putint(num:int);
fn putch(ch:int);

fn main()->int{
    let lb:int = ((1+2) <= 3);
    let lc:int = 0;
    foonull();
    barnull();
    foonull();
    ret 1+-lb+lc;
}

fn foonull(){
    let i:int = 0;
    while((i<10)){
        putint(gad[i]);
        putch(32); // blank
    }
    putch(10); // enter
}

fn barnull(){
    let i:int = 1;
    while((i<10)){
        gad[i] = i;
    }
    i = 1;
    while((i<10)){
        if((i == 8)){
            continue;
        }
        gad[i] = gad[i]*2;
    }
}
